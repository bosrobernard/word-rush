export const environment = {
  production: false,
  wsEndpoint:  'wss://gamecore.doronx.com',
  apiBase:     'https://gamecore.doronx.com',   // ← add this
  roomName:    '',
  guestModeEnabled: false,   // no longer needed but harmless to keep
};
